/*
package com.rezepte_app.S3;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import software.amazon.awssdk.services.s3.S3Client;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest
public class S3ConfigTest {

    @Autowired
    private S3Client s3Client;

    @Autowired
    private S3Config s3Config;

    @Test
    public void testS3ClientBeanCreation() {
        // Überprüfen, dass der S3Client bean korrekt erstellt wurde
        assertThat(s3Client).isNotNull();
    }

    @Test
    public void testGetBucketRegion() {
        // Überprüfen, ob die Region korrekt aus den S3Properties abgerufen wird
        assertThat(s3Config.getBucketRegion()).isEqualTo("eu-central-1"); // Ersetze dies durch deine tatsächliche Region
    }
}
*/

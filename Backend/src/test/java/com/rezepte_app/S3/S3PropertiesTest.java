/*
package com.rezepte_app.S3;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest
public class S3PropertiesTest {

    @Autowired
    private S3Properties s3Properties;

    @Test
    public void testS3Properties() {
        // Prüfe, ob die S3-Konfiguration korrekt geladen wurde
        assertThat(s3Properties).isNotNull();
        assertThat(s3Properties.getBucketName()).isEqualTo("bucket-bonn24");
        assertThat(s3Properties.getRegion()).isEqualTo("eu-central-1");
    }
}
*/

# Test-Datenbank (H2 In-Memory)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Hibernate: Erstellt und löscht die Tabellen bei jedem Testlauf
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=always

# Logging für Debugging aktivieren
spring.jpa.show-sql=true
logging.level.org.springframework.transaction=DEBUG

# Fake-Werte für Tests (z.B. Cognito)
aws.cognito.userPoolClientId=testClientId
test.user.email=gast@mdz.email
test.user.password=Gastzugang123!
